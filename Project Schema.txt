Flutter Project (Event Tracker) Schema:-

CREATE TABLE users (
    id TEXT PRIMARY KEY,              -- UUID or Firebase UID
    name TEXT NOT NULL,
    email TEXT UNIQUE NOT NULL,
    phone TEXT,
    dob TEXT,                         -- New: Date of Birth (ISO 8601 format: YYYY-MM-DD)
    avatar_url TEXT,
    role TEXT DEFAULT 'user',
    bio TEXT,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
);

CREATE TABLE events (
    id TEXT PRIMARY KEY,                   -- UUID
    title TEXT NOT NULL,
    description TEXT,
    start_datetime TEXT NOT NULL,          -- ISO 8601
    end_datetime TEXT,
    latitude REAL,                         -- Optional
    longitude REAL,                        -- Optional
    category_id INTEGER,
    is_completed INTEGER DEFAULT 0,
    is_reminder_set INTEGER DEFAULT 0,
    reminder_datetime TEXT,
    created_by TEXT NOT NULL,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,

    FOREIGN KEY (category_id) REFERENCES category(id),
    FOREIGN KEY (created_by) REFERENCES users(id)
);

CREATE TABLE tags (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
);

CREATE TABLE category (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    icon TEXT,                  -- Optional: store name of icon (e.g., 'work', 'personal')
);

CREATE TABLE events_tag (
    event_id TEXT,
    tag_id INTEGER,
    PRIMARY KEY (event_id, tag_id),
    FOREIGN KEY (event_id) REFERENCES events(id) ON DELETE CASCADE,
    FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE
);

CREATE TABLE eventviseUser (
    event_id TEXT,
    user_id TEXT,
    role TEXT DEFAULT 'invitee',       -- 'creator', 'invitee', 'editor'
    status TEXT DEFAULT 'pending',     -- 'accepted', 'declined', 'pending'
    num_adults INTEGER DEFAULT 1,      -- Number of adults (default to 1 - the user themselves)
    num_children INTEGER DEFAULT 0,    -- Number of children attending
    PRIMARY KEY (event_id, user_id),
    FOREIGN KEY (event_id) REFERENCES events(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);






----------------------------------------------------------------------------15/07/2025---------------------------------------------------------------------




Perfect. Here's the **final schema** with your original structure **plus my recommended additions (excluding optional/future ones)**. This version is well-structured for features like user management, login tracking, event control, and user-event relationships.

---

## ✅ Final Enhanced Schema for `event_tracker`

### 🔹 `users`

```sql
CREATE TABLE users (
    user_id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    email TEXT UNIQUE NOT NULL,
    phone TEXT,
    date_of_birth TEXT,
    avatar_url TEXT,
    gender TEXT,
    role TEXT DEFAULT 'user',
    bio TEXT,
    email_verified INTEGER DEFAULT 0,
    is_active INTEGER DEFAULT 1,
    last_login TEXT,
    created_at TEXT NOT NULL,
    updated_at TEXT
);
```

---

### 🔹 `events`

```sql
CREATE TABLE events (
    event_id TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    description TEXT,
    start_datetime TEXT NOT NULL,
    end_datetime TEXT,
    latitude REAL,
    longitude REAL,
    address TEXT,
    category_id INTEGER,
    event_type TEXT,                              -- cache of type name
    cover_image_url TEXT,
    max_capacity INTEGER,
    is_cancelled INTEGER DEFAULT 0,
    is_completed INTEGER DEFAULT 0,
    is_reminder_set INTEGER DEFAULT 0,
    reminder_datetime TEXT,
    created_by TEXT NOT NULL,
    created_at TEXT NOT NULL,
    updated_at TEXT,

    FOREIGN KEY (category_id) REFERENCES event_types(category_id),
    FOREIGN KEY (created_by) REFERENCES users(user_id)
);
```

---

### 🔹 `event_types`

```sql
CREATE TABLE event_types (
    category_id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    icon_name TEXT,
    color_code TEXT
);
```

---

### 🔹 `event_users`

```sql
CREATE TABLE event_users (
    event_id TEXT,
    user_id TEXT,
    is_admin INTEGER DEFAULT 0,
    is_verified INTEGER DEFAULT 0,
    role TEXT DEFAULT 'invitee',
    status TEXT DEFAULT 'pending',
    no_of_adults INTEGER DEFAULT 1,
    no_of_children INTEGER DEFAULT 0,
    response_datetime TEXT,
    note TEXT,
    created_at TEXT NOT NULL,
    updated_at TEXT,

    PRIMARY KEY (event_id, user_id),
    FOREIGN KEY (event_id) REFERENCES events(event_id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE
);
```

---

### ✅ Ready for Next Step

Let me know and I’ll now:

* Create the `lib/database/database.dart` file
* Implement:

  * Singleton DB class with init
  * `onCreate` SQL for all tables
  * Ready for insert/update/select helpers later

Shall I begin the code for `database.dart`?
